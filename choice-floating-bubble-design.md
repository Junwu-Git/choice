# 行动选项（choice）插件 —— 悬浮球（FloatingBubble）UI 设计方案

> 独立文档：与《choice-ui-redesign-spec.md》分开维护——那份已经在交给 Agent 执行中，本文档只针对悬浮球单独规划，不影响正在跑的主方案进度。
> 基线：基于 `src/components/FloatingBubble.vue`、`src/core/floating-state.ts`、`src/components/FloatingSettings.vue` 现有源码

---

## 1. 悬浮球（FloatingBubble）专项设计方案

### 1.1 现状功能盘点
`FloatingBubble.vue` 目前只做一件事：固定圆形按钮，可拖拽（用总位移辨别点击/拖拽），点击后 `openSettings()` 打开 `FloatingSettings.vue`（和主设置面板同一套 7 个 tab）。视觉上只有两个状态：默认（半透明玻璃圆 + 常驻发光阴影）和生成中（`choice-bubble-pulse` 脉冲）。图标是通用的 `fa-list-check`，和"行动选项/异步生成"这个产品概念关联度低。**没有"新结果待查看"状态，没有次要操作入口，点击行为单一。**

这是"能跑但没设计"的典型状态：不是丑，是没有信息传达——用户没法从悬浮球的外观判断"现在有没有新选项生成完成""要不要点进去看"。

### 1.2 设计目标
1. 悬浮球要能让人一眼识别这是"行动选项"功能入口，而不是通用设置齿轮
2. 平时收敛存在感（叠在聊天内容上层，不能常驻抢镜），有新东西时才该跳出来吸引注意力
3. 现代化但克制：靠状态清晰的分层视觉和恰到好处的动效，不是花哨堆砌

### 1.3 状态机（核心——把悬浮球从"一个开关"变成"一个状态指示器"）

| 状态 | 触发条件 | 视觉表现 |
| --- | --- | --- |
| Idle 空闲 | 无生成中，无未读结果 | 低调：贴边半隐藏（露出约40px），透明度略降，极慢速呼吸光晕（8s 周期） |
| Hover/触摸 | 指针悬停或触摸中 | 完全不透明，轻微放大，light glow 增强，露出完整图标 |
| Generating 生成中 | `generatorState.loading === true` | 保留现有 3s 脉冲动画，图标切换为小型 spinner，与 idle 呼吸动画区分开（两套独立 keyframes，不要共用） |
| 有新结果待查看（新增） | 最近一次 generation 完成时间 > 用户上次打开悬浮面板/气泡的时间 | 右上角小圆点/数字徽章，出现瞬间有弹入动效（scale + ease-out-back），颜色用 `--choice-text-hint`（琥珀色，与主色蓝区分，避免混淆） |
| Dragging 拖拽中 | 正在拖拽 | 沿用现有 `transition: none` + `will-change: transform` |
| Disabled 未配置 | 没有可用 API/条目池配置 | 轻微置灰 + 感叹号角标，减少新用户"点了没反应"的困惑 |

状态优先级互斥展示（不叠加造成视觉噪音）：`Disabled > Generating > 有新结果 > Dragging态 > Idle`。

这条状态机是本次设计里对现有功能唯一的行为级新增——"有新结果待查看"状态直接对应插件"异步生成、不阻塞、随时可回来看"的核心概念，之前完全没有可视化，属于设计缺口而不是锦上添花。

### 1.4 视觉规范（复用第 2 节 token，不新增色板）
- 尺寸维持 60px 触控热区（移动端友好基准），但引入"锚定态"缩小视觉体积：贴边吸附时只露出约 40px 直径，图标偏向可见的一侧，其余部分探出屏幕外
- 背景改两层结构：外层沿用现有 `--choice-bg-panel` + blur 玻璃底；内层加一圈渐变环（`conic-gradient` 或 `radial-gradient`，取 `--choice-primary`），动画速度/亮度随状态切换（idle 慢转、generating 快速脉冲、有新结果时常亮不转）
- 图标：从通用的 `fa-list-check` 换成更贴合"行动选项/分支选择"概念的 `fa-code-branch`（分支图标），或后续做一个简易自定义 SVG（三个圆点分叉，直观象征"多个选项"）；generating 时图标临时替换为 spinner，避免和脉冲动画叠加显得杂乱
- 徽章：小圆点起步，数量 >1 时显示数字（封顶 9+），尺寸约 18px，边框用当前主题背景色描边，避免和球体贴色糊在一起

### 1.5 交互设计
1. **点击行为拆分**：现在点击永远 `openSettings()`。改为——有未读结果时，点击优先展开"最近一次生成结果的快速预览"（复用 `ActionOptionsPanel` 的选项卡片样式，浮层展示，点选项直接可选），而不是直接跳设置弹窗；无未读结果时，点击行为不变，仍打开设置
2. **长按呼出快捷菜单**（>500ms）：气泡旁弹出小型菜单，提供"打开设置""隐藏悬浮球"两个入口，替代现在唯一路径
3. **拖拽松手自动吸附最近边缘**：现在只是 `clamp` 到边界内不越界，不吸附。改为松手后判断离左右边界哪侧更近，动画归位到贴边位置，呼应"平时收敛存在感"的目标，同时保留可点击区域

### 1.6 动效细节
- idle 呼吸光晕：8s 周期，极轻微，明显弱于 generating 的 3s 脉冲，两者要能一眼区分出"正常待机"和"正在跑"
- 新结果徽章出现：短促有力的弹入（`scale(0) → scale(1)`，`cubic-bezier(0.34, 1.56, 0.64, 1)`，约 0.3s），不能悄无声息地出现，否则用户不会注意到状态变化
- 吸附归位动画：同样用带回弹感的 easing，替代现有单调的 `0.3s ease-out`，是"现代化手感"里最直接能感知到的一处

### 1.7 FloatingSettings 弹窗联动
弹窗打开/关闭时的入场/退场动画，`transform-origin` 应对准悬浮球当前坐标（配合 `scale + opacity`），做出"设置面板是从这个球里生长出来的"空间关联感，而不是像现在这样在记忆坐标处直接生硬出现/消失。

### 1.8 实现要点（给 Agent）
- 新增 `computed hasUnseenResult`：比较最近一次 generation 完成时间戳与用户上次打开悬浮面板/点击气泡的时间戳
- 长按可直接用 `@vueuse/core` 的 `onLongPress`（已是依赖，见第 3 节判断），不必手写 `pointerdown`/`setTimeout`
- 吸附逻辑在现有 `useDraggable` 的 `onEnd` 回调里扩展：判断 `x` 距左右边界的距离，snap 到较近一侧，动画用上面 5.6 的 easing
- 弹窗 `transform-origin` 动画：在触发 `openSettings()` 前，把悬浮球当前 `x`/`y` 写入一个共享 ref（`floating-state.ts` 里加两个字段即可），`FloatingSettings.vue` 读取后设置为动画起点
