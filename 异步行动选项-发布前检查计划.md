# 异步行动选项(action-options) 插件 · 首个正式版发布前检查计划

> 交给 agent 执行说明：请按顺序逐项检查，每项标记 ✅通过 / ⚠️有问题(附说明) / ❌未实现。
> 发现的问题请在对应条目下追加简短记录，不要跳过未实现的项目直接标通过。

---

## 一、构建与打包

- [ ] `pnpm build`（非 watch 模式）能从干净状态一次性构建成功，无报错无警告
- [ ] 构建产物 `dist/index.js` 单文件体积在合理范围（记录实际大小，供后续版本对比）
- [ ] `manifest.json` 字段完整且正确：`display_name`、`version`、`author`、`homePage`、`js`（指向 dist 产物路径）等
- [ ] 清空 `node_modules` 后 `pnpm install && pnpm build` 能复现同样的构建结果（验证依赖锁定文件 `pnpm-lock.yaml` 有效）
- [ ] 确认 `.gitignore` 正确排除 `node_modules`、`dist`（如果 dist 不需要入库）、本地调试文件

## 二、核心功能 — 逐条对照需求清单验证

### 2.1 选项生成基础
- [ ] 副 API 单独调用生成选项，不影响/不触发主聊天生成
- [ ] 选项数量三种模式均正常：固定4、固定6、随机4-8（多次生成验证随机范围正确，不越界）
- [ ] 取消生成按钮：生成中可中止，中止后状态回归正常（不残留"生成中"死锁状态）
- [ ] 取消按钮已整合进现有 UI（不是独立悬浮按钮）

### 2.2 条目池与分级
- [ ] 聊天 / 角色 / 全局三级条目池均可独立增删改
- [ ] 覆盖优先级正确：聊天层有条目时，本轮完全不从角色层/全局层抽取
- [ ] 层级间快速插入/导入功能可用（把角色层或全局层条目复制到聊天层）
- [ ] 合并界面下按归属层级筛选查看，切换/筛选无误
- [ ] 固定(pinned)条目每轮必定发送，且正确从"随机抽取数量"中扣除（如总数4、固定1，则只随机抽3）
- [ ] 条目池数量小于设定抽取数量时的边界处理（比如池里只有2条但要求抽4条）不报错、有合理降级行为

### 2.3 上下文范围
- [ ] 按"轮数"（1层AI+1层user=1轮）设置上下文，取值验证：1轮、3轮等常规值结果正确
- [ ] 轮数设为 0 时，行为是"读取全部聊天历史"，而非"不带历史"

### 2.4 API 配置
- [ ] 可选择复用主API 或使用独立副API，两种模式切换正常
- [ ] 副API 配置支持保存多份，切换/删除/编辑互不影响
- [ ] API 配置信息本地持久化正确（刷新页面/重启前端后仍保留）

### 2.5 发送方式
- [ ] "直接发送"模式：点选项后立即发送，行为符合预期
- [ ] "填入输入框待编辑"模式：点选项后填入输入框但不自动发送，可编辑后手动发送
- [ ] 两种模式可通过开关正常切换，且切换后立即生效（不需要刷新）

### 2.6 生成历史翻页
- [ ] 同一楼层手动多次生成后，可翻页查看最近几次生成结果
- [ ] 翻页历史不跨楼层保留（切换/新增楼层后历史正确重置或不串层）

### 2.7 提示词模板
- [ ] 提示词编辑区可设置：人称/视角、选项边界范围、输出格式、每条字数等
- [ ] 占位符（轮数、固定条目、抽中条目等）在实际生成时被正确替换，替换结果符合预期
- [ ] 提示词为空或格式异常时，有合理默认值/报错提示（不会导致生成直接崩溃）

### 2.8 UI 与交互
- [ ] "游戏选择分支"按钮式风格在不同选项数量（4/6/8）下排版不错乱
- [ ] 可折叠面板挂载在最新 AI 楼层下方，折叠/展开状态正常
- [ ] 悬浮窗/独立按钮作为可选形式可用（如果已实现），与面板模式切换不冲突
- [ ] 面板/悬浮窗在窄屏（手机端浏览SillyTavern常见场景）下不溢出、不遮挡消息内容

## 三、独立性与依赖

- [ ] 未安装/未启用柏宝书插件时，本插件核心功能（生成、条目池、发送）完全不受影响，不报错
- [ ] 检查代码中确实没有对柏宝书内部 API/事件的强依赖（只是预留了后续联动的可能）

## 四、数据持久化

- [ ] 条目池（三级）、API配置、发送模式开关、面板展开状态等设置项刷新页面后保留
- [ ] 切换角色卡后，"角色层"条目池能正确切换为对应角色的数据，不串角色
- [ ] 切换/新建对话(chat)后，"聊天层"条目池正确隔离，不同聊天间不互相污染

## 五、错误处理与边界情况

- [ ] 副API 请求失败（超时/鉴权错误/返回格式异常）时有明确的用户可见提示，不是静默失败或卡死转圈
- [ ] 网络中断时重新生成，UI 能恢复到可重试状态
- [ ] 连续快速点击"生成"按钮，不会触发重复/并发请求导致状态错乱
- [ ] 极端输入验证：选项数量设置为负数/0/极大值时的边界处理

## 六、兼容性

- [ ] 至少在 Chrome 最新版验证一遍完整功能
- [ ] 全局扩展(global)与本地扩展(local)两种安装类型（如果都支持）均可正常加载
- [ ] 与已知会用到的其他插件（st-end-component-generator、压缩相邻消息脚本等）共存时无明显冲突（至少手动跑一遍基本场景）

## 七、文档与发布物

- [ ] README 包含：功能介绍、安装方式、基础使用截图或说明、已知限制
- [ ] `manifest.json` 版本号从开发版更新为正式版本号（如 `1.0.0`）
- [ ] Git 仓库打上对应版本 tag，CHANGELOG（如有）记录首个正式版内容
- [ ] 敏感信息检查：确认仓库/构建产物中没有硬编码的 API key、个人调试用的 baseURL 等

## 八、收尾验收

- [ ] 从"全新安装"角度走一遍完整流程：安装插件 → 配置API → 建条目池 → 生成选项 → 选择发送，全程无需查看源码或猜测即可完成
- [ ] 至少完整跑 2-3 轮真实对话场景，观察上下文轮数/固定条目/优先级等组合逻辑在真实使用中表现是否符合预期（不仅是单元测试式的孤立验证）

---

## 执行后请汇总

请在检查完所有项目后，额外汇总：
1. ❌ 未实现或 ⚠️ 有问题的条目列表（按严重程度排序：阻塞发布 / 可后续版本修复）
2. 是否有超出本清单范围但发现的其他问题
